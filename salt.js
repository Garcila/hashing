const bcrypt = require( 'bcrypt' );

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )

bcrypt.genSalt().then( salt => {
  console.log( 'salt:', salt );
} )